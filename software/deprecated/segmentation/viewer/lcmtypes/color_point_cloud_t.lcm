//lcm kinect ros
package lkr; 

struct color_point_cloud_t 
{
        lkr.header_t header; //contains the frame id
        int32_t num_points;
        lkr.point_xyzrgb_t points[num_points];
	int32_t height; //unsigned in pcl, but lcm doesn't have unsigned
	int32_t width; //unsigned in pcl, '''
}

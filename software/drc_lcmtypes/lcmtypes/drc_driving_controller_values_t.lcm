package drc;

struct driving_controller_values_t
{
    int64_t utime;
    int8_t throttle_value; //100 * ratio
    int16_t time_to_drive; //10* secs
    
    //we can prob ignore sending the brake value 
    int8_t brake_value; //100* ratio
    int8_t hand_steer;  //degrees
    int8_t goal_heading_angle; 
    int16_t goal_distance;
}

package drc;

// Additional Signals Exposed by BDI in 2.7.0 API:
// Same joint names and ordering as ATLAS_STATE
struct atlas_state_extra_t
{
  int64_t utime;

  int16_t num_joints;  
  float joint_position_out [num_joints];
  float joint_velocity_out [num_joints];
}

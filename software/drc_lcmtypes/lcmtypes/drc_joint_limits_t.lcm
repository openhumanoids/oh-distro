package drc;

struct joint_limits_t
{
    int64_t utime;

    int32_t num_joints;
    int32_t num_states;
    string joint_name[num_joints];

    // 0 = not at limit, 1 = at limit
    float joint_limit[num_states][num_joints];
}



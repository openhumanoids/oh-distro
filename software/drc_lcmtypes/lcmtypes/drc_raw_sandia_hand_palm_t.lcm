// For translating raw hand (RawHandState) msgs from ros to lcm
// Because LCM does not allow unsigned types, we do the following substitution
// uint16 -> int32
// uint32 -> int64


package drc;



struct raw_sandia_hand_palm_t
{
  int64_t utime;
  int64_t  palm_time;
  int16_t  palm_accel[3];
  int16_t  palm_gyro[3];
  int16_t  palm_mag[3];
  int32_t  palm_temps[7];
  int32_t  palm_tactile[32];
}

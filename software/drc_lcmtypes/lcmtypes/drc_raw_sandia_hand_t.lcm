// For translating raw hand (RawHandState) msgs from ros to lcm
// Because LCM does not allow unsigned types, we do the following substitution
// uint16 -> int32
// uint32 -> int64


package drc;

struct raw_sandia_hand_t
{
  int64_t utime;

  // Finger Order Assumed to be 
  // .... TODO ...
  int32_t num_fingers;
  raw_sandia_hand_finger_t fingers[num_fingers];
  raw_sandia_hand_palm_t palm;
  raw_sandia_hand_mobo_t mobo;
}

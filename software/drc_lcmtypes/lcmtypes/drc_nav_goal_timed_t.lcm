package drc;

// Base Goal Message with timeout

struct nav_goal_timed_t
{
	int64_t utime;

        // Amount of time that this goal is valid for (in nsec)
	int64_t timeout;
	string robot_name;
	position_3d_t goal_pos;
}


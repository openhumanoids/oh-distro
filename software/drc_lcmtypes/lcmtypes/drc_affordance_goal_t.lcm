package drc;

struct affordance_goal_t  //specifies a desired dof state
{
    int64_t utime;  
    string aff_type;   // name of the otdf file
    int32_t aff_uid;   // affordance uid
    int32_t num_dofs;
    string dof_name[num_dofs];   // joint name in the otdf file. 
    double dof_value[num_dofs];  // associated dof_value
}


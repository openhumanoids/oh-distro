package drc;

struct robot_state_constraint_checked_t
{
    int64_t utime;
    robot_state_t robot_state;

    // Additional fields for constraint checking
    const int8_t NOT_OK=0, OK=1;
    int32_t num_constraints;
    int8_t constraints_satisfied[num_constraints];
}


package drc;

struct map_controller_command_t
{
  // command to apply to controller-side map
  int8_t command;

  // command types
  const int8_t FULL_HEIGHTMAP = 1;   // default behavior; use actual heights
  const int8_t FLAT_GROUND = 2;      // use height at feet, normal = z_hat
}

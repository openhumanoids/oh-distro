package drc;

struct iris_region_request_t
{
	int64_t utime;

	// Robot state is needed to properly fill in missing terrain data
	robot_state_t initial_state;

	int32_t num_seed_poses;
	position_3d_t seed_poses[num_seed_poses];
}
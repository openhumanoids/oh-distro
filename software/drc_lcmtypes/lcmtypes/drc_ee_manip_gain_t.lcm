package drc;

// Published from the viewer via slider adjustment to control on COMMITTED_MANIP_GAIN
struct ee_manip_gain_t // Real adjustment of gains for cartesian error feedback to reduce SSE and also to compensate for unknown inertial properties of the manipuland
{
	 int64_t utime;
   boolean is_leg;      
   int16_t ee_type;  // see constants below 
   const int16_t LEFT=0, RIGHT=1, BOTH=2;
     
   double Kp;
   double Kd;
}


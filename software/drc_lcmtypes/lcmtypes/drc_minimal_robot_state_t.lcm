package drc;

// minimal version of robot state t:
// if you make changes to this message, map them to 
// the compariable changes in robot_state_t

struct minimal_robot_state_t
{
    int64_t utime;
    position_3d_t origin_position;  
    twist_t origin_twist;   
    int32_t num_joints;
    float joint_position [num_joints];
}


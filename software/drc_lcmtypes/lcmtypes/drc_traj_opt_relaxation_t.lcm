package drc;

struct traj_opt_relaxation_t  
{
    int64_t utime;
    string robot_name;
    
    int32_t num_links;
    string link_name [num_links];
    int64_t link_timestamps [num_links];
    
    int32_t num_joints;
    string joint_name [num_joints];
    int64_t joint_timestamps [num_joints];
}

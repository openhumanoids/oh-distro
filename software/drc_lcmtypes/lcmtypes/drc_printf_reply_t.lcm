package drc;

// This basic type used to reply
// with printf
// can be reconstituated into printf

struct printf_reply_t
{
   int32_t sheriff_id;
   boolean robot; // true if robot | false if extra
   int8_t n_printfs;
   string printfs[n_printfs];
}

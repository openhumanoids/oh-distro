package takktile;

// Basic state message, this assumes the standard 6 sensor configuration of
// robotiq takktile beta kit (three finger tips, three on palm)
struct state_t
{
  int64_t utime;

  int16_t id[30];
  int16_t force[30];
  int16_t temp[30];
}

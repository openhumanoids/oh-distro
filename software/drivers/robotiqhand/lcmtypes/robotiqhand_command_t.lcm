package robotiqhand;

// Basic high level commands
struct command_t
{
  int64_t utime;

  // 0 - basic (normal)
  // 1 - pinch
  // 2 - wide
  // 3 - scissor
  int8_t mode;

  // The range for all of these is 0-255
  int16_t position;
  int16_t force;
  int16_t velocity;
}

package viconstructs;
struct segment_t
{
    string name;
    // Global Rotation Euler (XYZ)
    double A[3];
    // Global Translation
    double T[3];

    // Local Rotation Euler (XYZ)
    double ba[3];
    // Local Translation
    double bt[3];

    // Don't know what these are - seem to be unfilled by the driver
    //double r[3];
    //double t[3];
}

# list of subdirectories to build, one one each line.  Empty lines
# and lines starting with '#' are ignored

state_sync
hand_estimate

vo_estimate

# Dehann's estimator:
twolegged_estimate
state_estimate

# TODO:
# some modules in the estimators have shared class names
# this is potentially conflicting
plan_monitor

signal_scope

vo_lidar_accum

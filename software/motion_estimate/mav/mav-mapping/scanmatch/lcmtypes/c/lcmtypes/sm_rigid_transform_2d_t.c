/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <string.h>
#include "lcmtypes/sm_rigid_transform_2d_t.h"

static int __sm_rigid_transform_2d_t_hash_computed;
static int64_t __sm_rigid_transform_2d_t_hash;

int64_t __sm_rigid_transform_2d_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __sm_rigid_transform_2d_t_get_hash)
            return 0;

    const __lcm_hash_ptr cp = { p, (void*)__sm_rigid_transform_2d_t_get_hash };
    (void) cp;

    int64_t hash = 0x09f1872c4f10ca88LL
         + __int64_t_hash_recursive(&cp)
         + __int64_t_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
         + __double_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __sm_rigid_transform_2d_t_get_hash(void)
{
    if (!__sm_rigid_transform_2d_t_hash_computed) {
        __sm_rigid_transform_2d_t_hash = __sm_rigid_transform_2d_t_hash_recursive(NULL);
        __sm_rigid_transform_2d_t_hash_computed = 1;
    }

    return __sm_rigid_transform_2d_t_hash;
}

int __sm_rigid_transform_2d_t_encode_array(void *buf, int offset, int maxlen, const sm_rigid_transform_2d_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].utime), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].utime_prev), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].pos, 2);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, &(p[element].theta), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_encode_array(buf, offset + pos, maxlen - pos, p[element].cov, 9);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int sm_rigid_transform_2d_t_encode(void *buf, int offset, int maxlen, const sm_rigid_transform_2d_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __sm_rigid_transform_2d_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __sm_rigid_transform_2d_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __sm_rigid_transform_2d_t_encoded_array_size(const sm_rigid_transform_2d_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int64_t_encoded_array_size(&(p[element].utime), 1);

        size += __int64_t_encoded_array_size(&(p[element].utime_prev), 1);

        size += __double_encoded_array_size(p[element].pos, 2);

        size += __double_encoded_array_size(&(p[element].theta), 1);

        size += __double_encoded_array_size(p[element].cov, 9);

    }
    return size;
}

int sm_rigid_transform_2d_t_encoded_size(const sm_rigid_transform_2d_t *p)
{
    return 8 + __sm_rigid_transform_2d_t_encoded_array_size(p, 1);
}

int __sm_rigid_transform_2d_t_decode_array(const void *buf, int offset, int maxlen, sm_rigid_transform_2d_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].utime), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].utime_prev), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].pos, 2);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, &(p[element].theta), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __double_decode_array(buf, offset + pos, maxlen - pos, p[element].cov, 9);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __sm_rigid_transform_2d_t_decode_array_cleanup(sm_rigid_transform_2d_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_decode_array_cleanup(&(p[element].utime), 1);

        __int64_t_decode_array_cleanup(&(p[element].utime_prev), 1);

        __double_decode_array_cleanup(p[element].pos, 2);

        __double_decode_array_cleanup(&(p[element].theta), 1);

        __double_decode_array_cleanup(p[element].cov, 9);

    }
    return 0;
}

int sm_rigid_transform_2d_t_decode(const void *buf, int offset, int maxlen, sm_rigid_transform_2d_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __sm_rigid_transform_2d_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __sm_rigid_transform_2d_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int sm_rigid_transform_2d_t_decode_cleanup(sm_rigid_transform_2d_t *p)
{
    return __sm_rigid_transform_2d_t_decode_array_cleanup(p, 1);
}

int __sm_rigid_transform_2d_t_clone_array(const sm_rigid_transform_2d_t *p, sm_rigid_transform_2d_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_clone_array(&(p[element].utime), &(q[element].utime), 1);

        __int64_t_clone_array(&(p[element].utime_prev), &(q[element].utime_prev), 1);

        __double_clone_array(p[element].pos, q[element].pos, 2);

        __double_clone_array(&(p[element].theta), &(q[element].theta), 1);

        __double_clone_array(p[element].cov, q[element].cov, 9);

    }
    return 0;
}

sm_rigid_transform_2d_t *sm_rigid_transform_2d_t_copy(const sm_rigid_transform_2d_t *p)
{
    sm_rigid_transform_2d_t *q = (sm_rigid_transform_2d_t*) malloc(sizeof(sm_rigid_transform_2d_t));
    __sm_rigid_transform_2d_t_clone_array(p, q, 1);
    return q;
}

void sm_rigid_transform_2d_t_destroy(sm_rigid_transform_2d_t *p)
{
    __sm_rigid_transform_2d_t_decode_array_cleanup(p, 1);
    free(p);
}

int sm_rigid_transform_2d_t_publish(lcm_t *lc, const char *channel, const sm_rigid_transform_2d_t *p)
{
      int max_data_size = sm_rigid_transform_2d_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = sm_rigid_transform_2d_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _sm_rigid_transform_2d_t_subscription_t {
    sm_rigid_transform_2d_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void sm_rigid_transform_2d_t_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    sm_rigid_transform_2d_t p;
    memset(&p, 0, sizeof(sm_rigid_transform_2d_t));
    status = sm_rigid_transform_2d_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding sm_rigid_transform_2d_t!!!\n", status);
        return;
    }

    sm_rigid_transform_2d_t_subscription_t *h = (sm_rigid_transform_2d_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    sm_rigid_transform_2d_t_decode_cleanup (&p);
}

sm_rigid_transform_2d_t_subscription_t* sm_rigid_transform_2d_t_subscribe (lcm_t *lcm,
                    const char *channel,
                    sm_rigid_transform_2d_t_handler_t f, void *userdata)
{
    sm_rigid_transform_2d_t_subscription_t *n = (sm_rigid_transform_2d_t_subscription_t*)
                       malloc(sizeof(sm_rigid_transform_2d_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 sm_rigid_transform_2d_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg sm_rigid_transform_2d_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int sm_rigid_transform_2d_t_subscription_set_queue_capacity (sm_rigid_transform_2d_t_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int sm_rigid_transform_2d_t_unsubscribe(lcm_t *lcm, sm_rigid_transform_2d_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe sm_rigid_transform_2d_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}


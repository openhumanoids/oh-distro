/** THIS IS AN AUTOMATICALLY GENERATED FILE.  DO NOT MODIFY
 * BY HAND!!
 *
 * Generated by lcm-gen
 **/

#include <string.h>
#include "lcmtypes/isam_slam_trajectory_t.h"

static int __isam_slam_trajectory_t_hash_computed;
static int64_t __isam_slam_trajectory_t_hash;

int64_t __isam_slam_trajectory_t_hash_recursive(const __lcm_hash_ptr *p)
{
    const __lcm_hash_ptr *fp;
    for (fp = p; fp != NULL; fp = fp->parent)
        if (fp->v == __isam_slam_trajectory_t_get_hash)
            return 0;

    const __lcm_hash_ptr cp = { p, (void*)__isam_slam_trajectory_t_get_hash };
    (void) cp;

    int64_t hash = 0x2ebb13a9f601a29eLL
         + __int64_t_hash_recursive(&cp)
         + __int32_t_hash_recursive(&cp)
         + __isam_slam_position_t_hash_recursive(&cp)
        ;

    return (hash<<1) + ((hash>>63)&1);
}

int64_t __isam_slam_trajectory_t_get_hash(void)
{
    if (!__isam_slam_trajectory_t_hash_computed) {
        __isam_slam_trajectory_t_hash = __isam_slam_trajectory_t_hash_recursive(NULL);
        __isam_slam_trajectory_t_hash_computed = 1;
    }

    return __isam_slam_trajectory_t_hash;
}

int __isam_slam_trajectory_t_encode_array(void *buf, int offset, int maxlen, const isam_slam_trajectory_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].utime), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_encode_array(buf, offset + pos, maxlen - pos, &(p[element].num_poses), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __isam_slam_position_t_encode_array(buf, offset + pos, maxlen - pos, p[element].poses, p[element].num_poses);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int isam_slam_trajectory_t_encode(void *buf, int offset, int maxlen, const isam_slam_trajectory_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __isam_slam_trajectory_t_get_hash();

    thislen = __int64_t_encode_array(buf, offset + pos, maxlen - pos, &hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    thislen = __isam_slam_trajectory_t_encode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int __isam_slam_trajectory_t_encoded_array_size(const isam_slam_trajectory_t *p, int elements)
{
    int size = 0, element;
    for (element = 0; element < elements; element++) {

        size += __int64_t_encoded_array_size(&(p[element].utime), 1);

        size += __int32_t_encoded_array_size(&(p[element].num_poses), 1);

        size += __isam_slam_position_t_encoded_array_size(p[element].poses, p[element].num_poses);

    }
    return size;
}

int isam_slam_trajectory_t_encoded_size(const isam_slam_trajectory_t *p)
{
    return 8 + __isam_slam_trajectory_t_encoded_array_size(p, 1);
}

int __isam_slam_trajectory_t_decode_array(const void *buf, int offset, int maxlen, isam_slam_trajectory_t *p, int elements)
{
    int pos = 0, thislen, element;

    for (element = 0; element < elements; element++) {

        thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].utime), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        thislen = __int32_t_decode_array(buf, offset + pos, maxlen - pos, &(p[element].num_poses), 1);
        if (thislen < 0) return thislen; else pos += thislen;

        p[element].poses = (isam_slam_position_t*) lcm_malloc(sizeof(isam_slam_position_t) * p[element].num_poses);
        thislen = __isam_slam_position_t_decode_array(buf, offset + pos, maxlen - pos, p[element].poses, p[element].num_poses);
        if (thislen < 0) return thislen; else pos += thislen;

    }
    return pos;
}

int __isam_slam_trajectory_t_decode_array_cleanup(isam_slam_trajectory_t *p, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_decode_array_cleanup(&(p[element].utime), 1);

        __int32_t_decode_array_cleanup(&(p[element].num_poses), 1);

        __isam_slam_position_t_decode_array_cleanup(p[element].poses, p[element].num_poses);
        if (p[element].poses) free(p[element].poses);

    }
    return 0;
}

int isam_slam_trajectory_t_decode(const void *buf, int offset, int maxlen, isam_slam_trajectory_t *p)
{
    int pos = 0, thislen;
    int64_t hash = __isam_slam_trajectory_t_get_hash();

    int64_t this_hash;
    thislen = __int64_t_decode_array(buf, offset + pos, maxlen - pos, &this_hash, 1);
    if (thislen < 0) return thislen; else pos += thislen;
    if (this_hash != hash) return -1;

    thislen = __isam_slam_trajectory_t_decode_array(buf, offset + pos, maxlen - pos, p, 1);
    if (thislen < 0) return thislen; else pos += thislen;

    return pos;
}

int isam_slam_trajectory_t_decode_cleanup(isam_slam_trajectory_t *p)
{
    return __isam_slam_trajectory_t_decode_array_cleanup(p, 1);
}

int __isam_slam_trajectory_t_clone_array(const isam_slam_trajectory_t *p, isam_slam_trajectory_t *q, int elements)
{
    int element;
    for (element = 0; element < elements; element++) {

        __int64_t_clone_array(&(p[element].utime), &(q[element].utime), 1);

        __int32_t_clone_array(&(p[element].num_poses), &(q[element].num_poses), 1);

        q[element].poses = (isam_slam_position_t*) lcm_malloc(sizeof(isam_slam_position_t) * q[element].num_poses);
        __isam_slam_position_t_clone_array(p[element].poses, q[element].poses, p[element].num_poses);

    }
    return 0;
}

isam_slam_trajectory_t *isam_slam_trajectory_t_copy(const isam_slam_trajectory_t *p)
{
    isam_slam_trajectory_t *q = (isam_slam_trajectory_t*) malloc(sizeof(isam_slam_trajectory_t));
    __isam_slam_trajectory_t_clone_array(p, q, 1);
    return q;
}

void isam_slam_trajectory_t_destroy(isam_slam_trajectory_t *p)
{
    __isam_slam_trajectory_t_decode_array_cleanup(p, 1);
    free(p);
}

int isam_slam_trajectory_t_publish(lcm_t *lc, const char *channel, const isam_slam_trajectory_t *p)
{
      int max_data_size = isam_slam_trajectory_t_encoded_size (p);
      uint8_t *buf = (uint8_t*) malloc (max_data_size);
      if (!buf) return -1;
      int data_size = isam_slam_trajectory_t_encode (buf, 0, max_data_size, p);
      if (data_size < 0) {
          free (buf);
          return data_size;
      }
      int status = lcm_publish (lc, channel, buf, data_size);
      free (buf);
      return status;
}

struct _isam_slam_trajectory_t_subscription_t {
    isam_slam_trajectory_t_handler_t user_handler;
    void *userdata;
    lcm_subscription_t *lc_h;
};
static
void isam_slam_trajectory_t_handler_stub (const lcm_recv_buf_t *rbuf,
                            const char *channel, void *userdata)
{
    int status;
    isam_slam_trajectory_t p;
    memset(&p, 0, sizeof(isam_slam_trajectory_t));
    status = isam_slam_trajectory_t_decode (rbuf->data, 0, rbuf->data_size, &p);
    if (status < 0) {
        fprintf (stderr, "error %d decoding isam_slam_trajectory_t!!!\n", status);
        return;
    }

    isam_slam_trajectory_t_subscription_t *h = (isam_slam_trajectory_t_subscription_t*) userdata;
    h->user_handler (rbuf, channel, &p, h->userdata);

    isam_slam_trajectory_t_decode_cleanup (&p);
}

isam_slam_trajectory_t_subscription_t* isam_slam_trajectory_t_subscribe (lcm_t *lcm,
                    const char *channel,
                    isam_slam_trajectory_t_handler_t f, void *userdata)
{
    isam_slam_trajectory_t_subscription_t *n = (isam_slam_trajectory_t_subscription_t*)
                       malloc(sizeof(isam_slam_trajectory_t_subscription_t));
    n->user_handler = f;
    n->userdata = userdata;
    n->lc_h = lcm_subscribe (lcm, channel,
                                 isam_slam_trajectory_t_handler_stub, n);
    if (n->lc_h == NULL) {
        fprintf (stderr,"couldn't reg isam_slam_trajectory_t LCM handler!\n");
        free (n);
        return NULL;
    }
    return n;
}

int isam_slam_trajectory_t_subscription_set_queue_capacity (isam_slam_trajectory_t_subscription_t* subs,
                              int num_messages)
{
    return lcm_subscription_set_queue_capacity (subs->lc_h, num_messages);
}

int isam_slam_trajectory_t_unsubscribe(lcm_t *lcm, isam_slam_trajectory_t_subscription_t* hid)
{
    int status = lcm_unsubscribe (lcm, hid->lc_h);
    if (0 != status) {
        fprintf(stderr,
           "couldn't unsubscribe isam_slam_trajectory_t_handler %p!\n", hid);
        return -1;
    }
    free (hid);
    return 0;
}


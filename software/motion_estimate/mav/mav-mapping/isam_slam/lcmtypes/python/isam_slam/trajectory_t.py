"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

import cStringIO as StringIO
import struct

import position_t

class trajectory_t(object):
    __slots__ = ["utime", "num_poses", "poses"]

    def __init__(self):
        self.utime = 0
        self.num_poses = 0
        self.poses = []

    def encode(self):
        buf = StringIO.StringIO()
        buf.write(trajectory_t._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">qi", self.utime, self.num_poses))
        for i0 in range(self.num_poses):
            assert self.poses[i0]._get_packed_fingerprint() == position_t.position_t._get_packed_fingerprint()
            self.poses[i0]._encode_one(buf)

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = StringIO.StringIO(data)
        if buf.read(8) != trajectory_t._get_packed_fingerprint():
            raise ValueError("Decode error")
        return trajectory_t._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = trajectory_t()
        self.utime, self.num_poses = struct.unpack(">qi", buf.read(12))
        self.poses = []
        for i0 in range(self.num_poses):
            self.poses.append(position_t.position_t._decode_one(buf))
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if trajectory_t in parents: return 0
        newparents = parents + [trajectory_t]
        tmphash = (0x2ebb13a9f601a29e+ position_t.position_t._get_hash_recursive(newparents)) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff)  + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if trajectory_t._packed_fingerprint is None:
            trajectory_t._packed_fingerprint = struct.pack(">Q", trajectory_t._get_hash_recursive([]))
        return trajectory_t._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)


/* LCM type definition class file
 * This file was automatically generated by lcm-gen
 * DO NOT MODIFY BY HAND!!!!
 */

package drc;
 
import java.io.*;
import java.util.*;
import lcm.lcm.*;
 
public final class robot_body_t implements lcm.lcm.LCMEncodable
{
    public long utime;
    public String robot_name;
    public int num_bodies;
    public String body_name[];
    public int body_flag[];
 
    public robot_body_t()
    {
    }
 
    public static final long LCM_FINGERPRINT;
    public static final long LCM_FINGERPRINT_BASE = 0x81b0deeb58c1d8fdL;
 
    static {
        LCM_FINGERPRINT = _hashRecursive(new ArrayList<Class<?>>());
    }
 
    public static long _hashRecursive(ArrayList<Class<?>> classes)
    {
        if (classes.contains(drc.robot_body_t.class))
            return 0L;
 
        classes.add(drc.robot_body_t.class);
        long hash = LCM_FINGERPRINT_BASE
            ;
        classes.remove(classes.size() - 1);
        return (hash<<1) + ((hash>>63)&1);
    }
 
    public void encode(DataOutput outs) throws IOException
    {
        outs.writeLong(LCM_FINGERPRINT);
        _encodeRecursive(outs);
    }
 
    public void _encodeRecursive(DataOutput outs) throws IOException
    {
        char[] __strbuf = null;
        outs.writeLong(this.utime); 
 
        __strbuf = new char[this.robot_name.length()]; this.robot_name.getChars(0, this.robot_name.length(), __strbuf, 0); outs.writeInt(__strbuf.length+1); for (int _i = 0; _i < __strbuf.length; _i++) outs.write(__strbuf[_i]); outs.writeByte(0); 
 
        outs.writeInt(this.num_bodies); 
 
        for (int a = 0; a < this.num_bodies; a++) {
            __strbuf = new char[this.body_name[a].length()]; this.body_name[a].getChars(0, this.body_name[a].length(), __strbuf, 0); outs.writeInt(__strbuf.length+1); for (int _i = 0; _i < __strbuf.length; _i++) outs.write(__strbuf[_i]); outs.writeByte(0); 
        }
 
        for (int a = 0; a < this.num_bodies; a++) {
            outs.writeInt(this.body_flag[a]); 
        }
 
    }
 
    public robot_body_t(byte[] data) throws IOException
    {
        this(new LCMDataInputStream(data));
    }
 
    public robot_body_t(DataInput ins) throws IOException
    {
        if (ins.readLong() != LCM_FINGERPRINT)
            throw new IOException("LCM Decode error: bad fingerprint");
 
        _decodeRecursive(ins);
    }
 
    public static drc.robot_body_t _decodeRecursiveFactory(DataInput ins) throws IOException
    {
        drc.robot_body_t o = new drc.robot_body_t();
        o._decodeRecursive(ins);
        return o;
    }
 
    public void _decodeRecursive(DataInput ins) throws IOException
    {
        char[] __strbuf = null;
        this.utime = ins.readLong();
 
        __strbuf = new char[ins.readInt()-1]; for (int _i = 0; _i < __strbuf.length; _i++) __strbuf[_i] = (char) (ins.readByte()&0xff); ins.readByte(); this.robot_name = new String(__strbuf);
 
        this.num_bodies = ins.readInt();
 
        this.body_name = new String[(int) num_bodies];
        for (int a = 0; a < this.num_bodies; a++) {
            __strbuf = new char[ins.readInt()-1]; for (int _i = 0; _i < __strbuf.length; _i++) __strbuf[_i] = (char) (ins.readByte()&0xff); ins.readByte(); this.body_name[a] = new String(__strbuf);
        }
 
        this.body_flag = new int[(int) num_bodies];
        for (int a = 0; a < this.num_bodies; a++) {
            this.body_flag[a] = ins.readInt();
        }
 
    }
 
    public drc.robot_body_t copy()
    {
        drc.robot_body_t outobj = new drc.robot_body_t();
        outobj.utime = this.utime;
 
        outobj.robot_name = this.robot_name;
 
        outobj.num_bodies = this.num_bodies;
 
        outobj.body_name = new String[(int) num_bodies];
        if (this.num_bodies > 0)
            System.arraycopy(this.body_name, 0, outobj.body_name, 0, this.num_bodies); 
        outobj.body_flag = new int[(int) num_bodies];
        if (this.num_bodies > 0)
            System.arraycopy(this.body_flag, 0, outobj.body_flag, 0, this.num_bodies); 
        return outobj;
    }
 
}

